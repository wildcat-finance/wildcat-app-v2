# Known app issues found by the e2e UAT suites

Policy: the test suite documents app defects instead of fixing them — tests carry workarounds or
`test.fixme` / `test.fail` entries referencing this file by number. App fixes live on separate
branches, never in the test-suite history. Numbers are stable and never reused.

Each row carries one machine header in its last cell, wrapped in an HTML comment (opens `<!--`, closes
`-->`) so GitHub renders nothing: `ki: id=<n> severity=blocker|high|medium|low
status=open|fixed-upstream|fixed-verified|not-a-defect|owner-call|closed tests=<UAT ids, comma separated, or ->
blocks=<release>|no [decision=pending]`. The UAT report parses it into the release view
(`e2e/lib/knownIssues.ts`); a new row without one is invisible to the report.

Pins: #1–#8 were found on app ae0335f3 + SDK 3.2.4-beta + subgraph 2.5.9 + Sepolia fork @ 11584253;
everything else, and every "fixed" verdict, is against `harness/fork/pins.json` (app d335b59b +
SDK 3.2.7-beta + subgraph v2.5.11, same fork block). Tests referenced are those that carry the
workaround or prove the status.

The `main`-variant branch (`feat/local-fork-harness-main`) has its own `e2e/KNOWN-ISSUES.md` with
the main-only findings (`M*`) and the harness limits that constrain both boards (`H*`).

| # | Finding | Status | Tests |
|---|---|---|---|
| 1 | Deep links to wallet-gated routes bounce during wallet reconnect: `useNetworkGate` redirects `NO_WALLET_RESTRICTED_PATHS` to `/` while `address` is briefly undefined; worst case an unsigned borrower lands on `/lender/agreement` and signs the LENDER ToU. Deterministic for `/lender/my-markets` in a fresh context on head. | open; proposed fix on branch `fix/wallet-settling-deep-links` (hold the redirect while connecting) | suites connect first, then navigate (`gotoGatedBorrowerPath`, `connectAs`) <!-- ki: id=1 severity=medium status=open tests=- blocks=no --> |
| 2 | `/borrower/create-market` deep link: SSR hydration mismatch (button-in-div); React remounts and #1 then bounces the browser mid-form. | open | wizard entered via the dashboard CTA <!-- ki: id=2 severity=medium status=open tests=- blocks=no --> |
| 3 | Policies sidebar item is a bare Typography span (no accessible role); the policies list's "Assigned to Markets" column renders empty. | open | role queries avoided for that item <!-- ki: id=3 severity=low status=open tests=- blocks=no --> |
| 4 | Borrowers could not see their own allowlist: the policy Lenders tab and market Lenders section rendered from `LenderHooksAccess`, and pull providers mint credentials only on first use. | substantially fixed upstream (0fbe365c, 40108a17): members listed with an Access Source column, duplicate rejection, removal controls. Striking a member sends `blockFromDeposits` to the hooks instance; the access-list membership persists. | BOP-03 / BOP-03b assert `isBlockedFromDeposits` <!-- ki: id=4 severity=medium status=fixed-upstream tests=BOP-03,BOP-03b blocks=no --> |
| 5 | "Transaction Successful!" shown for a REVERTED borrow: a stale short gas estimate made the second borrow run out of gas and the modal never checked `receipt.status`. | fixed upstream (ef28a128: reject reverted receipts, buffer SDK gas); verified — no reverted transactions on head boards | `borrowThroughUi` verifies the on-chain balance delta <!-- ki: id=5 severity=high status=fixed-verified tests=- blocks=no --> |
| 6 | Days-mode repay never enabled: the modal gated Repay on "Amount To Repay" (creeping per second) while Approve approved a different amount. | fixed upstream (`RepayModal` pins the approved quote) | BOP-09b proves it at runtime <!-- ki: id=6 severity=high status=fixed-upstream tests=BOP-09b blocks=no --> |
| 7 | The borrower chip on lender market rows opened `/lender/profile/<borrower>`, i.e. the CONNECTED lender's own profile. | fixed upstream (24624183): chips link to `/profile/borrower/<addr>`; the legacy route redirects there | LEN-07, LEN-07b <!-- ki: id=7 severity=low status=fixed-upstream tests=LEN-07,LEN-07b blocks=no --> |
| 8 | Blanket "cannot use markets as a lender" warning: SDK 3.2.5-beta's V2.5 lens path reports pull-provider access as `canRefresh` but credential validity still required a stored timestamp. | fixed in SDK 3.2.6-beta (`isCredentialValid`); legacy-market lens errors fixed in 3.2.7-beta | — <!-- ki: id=8 severity=medium status=fixed-upstream tests=- blocks=no --> |
| 9 | HARNESS: fork subgraph 2.5.9 could not serve the SDK's market-history query (`WithdrawalExecution.eventIndex` missing), so Market History rendered empty. | closed: the fork subgraph is v2.5.11 | LEN-10's schema probe arms its records-table asserts <!-- ki: id=9 severity=low status=closed tests=LEN-10 blocks=no --> |
| 10 | Profile edits render stale after save: `GET /api/profiles/[address]` is served `public, s-maxage=300, stale-while-revalidate=600`, so the post-save refetch can be answered with the pre-edit body; behind a CDN every viewer sees the old profile for up to 5 min (+10 min SWR). | open | BON-08 reload-polls and journals `reloadsUntilFreshProfile` <!-- ki: id=10 severity=medium status=open tests=BON-08 blocks=no --> |
| 11 | Fully-subscribed markets vanish from Explore and Trending: `isExploreVisible` (`src/utils/marketStatus.ts`) requires `maxTotalSupply > totalSupply`. `capacityLeft` is computed non-saturating and goes negative on interest overshoot (masked by the cells). | open; product confirmation against the Glass Door spec | EDG-08 (observational) <!-- ki: id=11 severity=medium status=open tests=EDG-08 blocks=no decision=pending --> |
| 12 | Penalty contagion: `getPenaltyBorrowers` removes EVERY market of a borrower with any market in penalty from Explore and Trending. | open; possibly intended (reputational gating) | EDG-08 <!-- ki: id=12 severity=medium status=open tests=EDG-08 blocks=no --> |
| 13 | A failed token APPROVE in the deposit modal has no retry path (only the deposit mutation is wired to the ErrorModal); a wallet-REJECTED approve (EIP-1193 4001) shows no surface at all — no modal, no toast — while the deposit leg handles the same rejection correctly. | open | EDG-13 (deposit leg), WAL-07 journals the approve gap <!-- ki: id=13 severity=medium status=open tests=EDG-13,WAL-07 blocks=no --> |
| 14 | Subgraph-derived views poll at 60 s (`INDEXED_ACCOUNT_POLLING_INTERVAL`) with no staleness indicator. Boards run at the test-mode cadence, so this edge is measured, not exercised. | design edge | EDG-10 journals the lag <!-- ki: id=14 severity=low status=not-a-defect tests=EDG-10 blocks=no --> |
| 15 | The lender market page re-dispatches `setSection` whenever wallet/access inputs settle, discarding a section the user already chose; the sidebar collapses to skeletons on every indexed refetch. | open | `openSection` re-clicks until the section sticks <!-- ki: id=15 severity=medium status=open tests=- blocks=no --> |
| 16 | The borrower-side ToU prompt auto-opens ~0.3 s after the page is interactive and swallows in-flight clicks; while it is up the header is aria-hidden. | open | tests dismiss blockers and retry <!-- ki: id=16 severity=medium status=open tests=- blocks=no --> |
| 17 | Dev and production builds show different catalogues: `EXCLUDED_BORROWERS` / `EXCLUDED_MARKETS` are emptied when `NODE_ENV=development`. | upstream behaviour, not a defect; catalogue counts are only comparable within one build mode | oracles probe the build mode <!-- ki: id=17 severity=low status=not-a-defect tests=- blocks=no --> |
| 18 | The Explore penalty blacklist (#12) self-heals invisibly: `timeDelinquent` decays on-chain, so a borrower's whole set disappears and reappears with no user action. | extends #12 | — <!-- ki: id=18 severity=low status=open tests=- blocks=no --> |
| 19 | Indexed `isIncurringPenalties` / `timeDelinquent` are as-of-last-event and can disagree with the chain by orders of magnitude. The app derives delinquency from lens reads and is immune; other consumers of the subgraph are not. | not an app defect | oracles read `currentState()` <!-- ki: id=19 severity=low status=not-a-defect tests=- blocks=no --> |
| 20 | Periodic APR reduction cannot be APPLIED through the app: the permissionless `executePendingAnnualInterestBipsReduction` reverts "SphereX error: disallowed tx pattern" on its success path (call-flow allowlist has no pattern for the selector, which is new in v2.5), while the borrower's `setAnnualInterestAndReserveRatioBips` routes the same hook call and succeeds. The app's Settle-and-Apply plan only routes the permissionless function. | open; deployment configuration (SphereX allowlist), app has no fallback | BOP-17 journals the revert and falls back; BOP-17b is a `test.fail` known-blocked row <!-- ki: id=20 severity=blocker status=open tests=BOP-17,BOP-17b blocks=2.5 --> |
| 21 | Borrower-side "Download Signed MLA" (`GET /api/mla/<market>/signed`) serves the borrower pre-signature with lender placeholders unresolved; the fully executed document exists only per lender (`GET /api/mla/<market>/<lender>/signed`, a ZIP). | product decision: should the borrower get an all-lenders executed bundle? | BOP-27 asserts the actual shapes <!-- ki: id=21 severity=medium status=owner-call tests=BOP-27 blocks=no decision=pending --> |
| 22 | After a UI invitation-accept, `findBorrowerWithPendingInvitation` once read `isRegisteredBorrower` as true and persisted `registeredOnChain=true`, hiding the pending invite (`inviteHead` 404) until the row was reset. | observed on two fresh boards (BON-03, on both apps), never on a rerun of the suite | — <!-- ki: id=22 severity=medium status=open tests=- blocks=no --> |
| 23 | During a full-exit withdrawal burn the market's scaled `balanceOf` reverted with an arithmetic under/overflow for one block, then read healthy. | observed once (LEN-21); cause unconfirmed | lib view reads tolerate a transient revert and keep polling <!-- ki: id=23 severity=low status=open tests=- blocks=no --> |
