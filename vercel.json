{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install -g npm@11.12.0 --ignore-scripts --audit=false --fund=false && node --version && npm --version && node scripts/check-exact-versions.mjs && npm ci --ignore-scripts --audit=false --fund=false",
  "buildCommand": "npx --no -- puppeteer browsers install chrome && npm run build"
}
