engine-strict=true
ignore-scripts=true
save-exact=true
save-prefix=
min-release-age=7
allow-git=none
# Some runtime and test dependencies do not yet declare React 19-compatible peer ranges,
# including the legacy Safe SDK, wagmi's MetaMask connector, and Testing Library.
# Remove this after those dependencies are upgraded or replaced.
legacy-peer-deps=true
