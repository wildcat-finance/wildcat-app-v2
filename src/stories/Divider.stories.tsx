import { Divider } from "@mui/material"
import type { Meta } from "@storybook/react"

export default {
  title: "Components/Divider",
  component: Divider,
} as Meta<typeof Divider>

export const ThemedDivider = () => <Divider />
