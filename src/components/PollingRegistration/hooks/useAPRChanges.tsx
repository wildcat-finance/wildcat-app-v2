import { getMarketRecords, getMarketsForBorrower } from "@wildcatfi/wildcat-sdk"

import { SubgraphClient } from "@/config/subgraph"

export const useAPRChanges = (address?: string) => {
  const fetchAPRChanges = async () => {
    // if (address) {
    // }
  }

  return fetchAPRChanges
}
