export type SmallFilterSelectItem = { id: string; name: string }

export const subAssetIds = ["healthy", "pending", "penalty"]
export const parentAssetId = "market"

export const subStatusIds = ["eug", "weth", "uni"]
export const parentStatusId = "currency"
