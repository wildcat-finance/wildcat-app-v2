import * as React from "react"

import {
  MobileMarketCard,
  MobileMarketCardProps,
} from "@/components/Mobile/MobileMarketCard"

export const RevolvingMarketCard = (props: MobileMarketCardProps) => (
  <MobileMarketCard {...props} />
)
